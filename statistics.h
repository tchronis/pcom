/*
 * statistics.h
 *
 *  Created on: Jul 19, 2014
 *      Author: tchr
 */

#ifndef STATISTICS_H_
#define STATISTICS_H_

long double min(vector <long double>);

long double max(vector <long double>);

long double mean(vector <long double>);

long double st_dev(vector <long double>);

long double adev(vector <long double>);

long double skew(vector <long double>);

#endif /* STATISTICS_H_ */
